# Oracle User Prompt — v0 PLACEHOLDER
# Status: NOT READY. Person 1 must write and commit the real prompt here
#         before any Condition C run is executed.
#
# Variables injected at runtime:
#   {cluster_summary}   — same format as interpreter prompt
#   {turn_number}       — current turn (1–5)
#
# The model plays the role of a domain-naive analyst who examines the cluster
# output and issues ONE natural-language refinement instruction per turn.
# The instruction must be a plain English sentence (not JSON).
# On turn 5, if the analyst is satisfied, respond with: DONE

YOU ARE A PLACEHOLDER. REPLACE THIS ENTIRE FILE BEFORE RUNNING ANY EXPERIMENT.
